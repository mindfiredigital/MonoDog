{
  "name": "@monodog/monorepo-scanner",
  "version": "1.0.8",
  "description": "Monorepo scanner for analyzing packages, dependencies, and health",
  "main": "dist/index.js",
   "license": "MIT",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "build": "tsc",
    "test": "jest",
    "clean": "rm -rf dist node_modules/.cache",
    "lint": "eslint .",
    "test:coverage": "jest --coverage"
  },
  "dependencies": {
    "@prisma/client": "^5.7.0",
    "@monodog/utils": "1.0.1"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "tsx": "^4.6.0",
    "typescript": "^5.3.0",
    "prisma": "^5.7.0"
  },
  "peerDependencies": {
  }
}
