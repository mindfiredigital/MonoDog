{
  "name": "@lakinmindfire/tsconfig",
  "version": "0.0.0",
  "description": "Shared TypeScript configurations for the Monorepo Dashboard.",
  "main": "base.json",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "echo \"Configuration package build success (no code compilation)\"",
    "lint": "eslint .",
    "test:coverage": "jest --coverage"
  },
  "files": [
    "base.json",
    "react.json",
    "node.json"
  ]
}
